;define(["av"],function(av){
    "use strict";
    var $id = "layout.user.index";

    // viewModel
    var vm = av.define({
        $id : $id+".vm",
        view : {
        }
    });

    vm.scan();

});